# Local Docker integration settings only. Never reuse these values in Production.
SA_PASSWORD=replace-with-a-long-unique-local-sql-sa-password
ADMIN_EMAIL=owner@example.test
SOURCE_REVISION_ID=local-compose

# Optional local SMTP settings. Leave blank when email delivery is not under test.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
